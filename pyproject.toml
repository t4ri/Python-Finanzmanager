[tool.poetry]
name = "python-finanzmanager"
version = "1.1.0"
description = ""
authors = ["t4ri"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
pdfplumber = "^0.11.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
